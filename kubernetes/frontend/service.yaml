apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: innovation-intelligence
  labels:
    app: frontend
spec:
  type: ClusterIP
  selector:
    app: frontend
    component: web
  ports:
  - name: http
    port: 80
    targetPort: 8080
    protocol: TCP
